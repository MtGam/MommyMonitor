<%= form_for :comment do |f| %>
  <%= f.text_area :comment, size: '25x10', placeholder: 'Please write your comments here!' %>
    <spellcheck="true">
    <%= f.hidden_field :mother_id, :value => @mother_id %>
  <%= f.submit %>
<% end %>
